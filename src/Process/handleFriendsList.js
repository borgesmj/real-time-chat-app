import { doc, updateDoc } from "firebase/firestore";
import { db } from "./Firebase";

export const addFriend = async (currentUserDocID, friendsList) => {
    console.log('debes enviarlo a la lista de request')
};
