const ChatName = ({darkTheme}) => {
  return (
    <div className={`flex justify-between items-center w-full bg-[var(--primary-200)] h-12 md:h-20`}>
      una chat app
    </div>
  )
}

export default ChatName
